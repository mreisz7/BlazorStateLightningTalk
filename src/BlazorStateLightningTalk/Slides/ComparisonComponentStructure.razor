@inherits SlideBase

<div class="full-screen">
    <header class="slide-header">
        <h1 class="slide-title">Comparison: Component Structure</h1>
        <img class="logo"
             src="img/react-logo.png" />
        <img class="logo"
             src="img/blazor-logo.png" />
    </header>
    <div class="slide-content split">
        <div class="split-left">
            <h2>React:</h2>
            <codeblock Class="@ShowSlideStopElement(1)"
                       FileName="button.jsx">
                @CodeStrings.buttonJsx
            </codeblock>
        </div>
        <div class="split-right">
            <h2>Blazor:</h2>
            <codeblock Class="@ShowSlideStopElement(2)"
                       FileName="Button.razor">
                @CodeStrings.buttonRazor
            </codeblock>
        </div>
    </div>
</div>


@code {
    public override int SlideStopMax { get; set; } = 2;
}
